/* import Sequelize from "sequelize";
import { Config } from "../config/config.js";

Config.InstallServerConfig()

export const db = new Sequelize(Config.postgresConfig.postgres_db_db_name,'postgres','KiirlmaNoktasi42',{
    host:Config.postgresConfig.postgres_db_host,
    dialect:'postgres'
})
 */